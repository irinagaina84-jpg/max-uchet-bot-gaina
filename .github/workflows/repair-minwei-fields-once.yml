name: Repair Min Wei CRM fields once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: One-time diagnostic disabled
        run: echo 'Min Wei field repair is manual-only; robust importer now uses existing Bitrix field technical codes.'
