name: Legacy Bitrix raw cleanup (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy raw cleanup disabled. Current Bitrix policy accepts finalized working files only.'
