name: Configure Bitrix CRM containers

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Multi-funnel setup is disabled because the current Bitrix24 plan does not support crm.category.add."
