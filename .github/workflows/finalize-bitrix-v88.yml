name: Finalize Bitrix clean structure (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Bitrix clean structure is finalized. Manual maintenance only.'
