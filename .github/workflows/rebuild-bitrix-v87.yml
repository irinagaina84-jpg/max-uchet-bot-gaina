name: Organize Bitrix working files (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  organize:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Bitrix structure is fixed. Run manually only when a full rebuild is intentionally needed.'
