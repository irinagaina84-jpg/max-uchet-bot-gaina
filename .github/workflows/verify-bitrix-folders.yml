name: Legacy Bitrix folder bootstrap (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy bootstrap disabled. Use the current Bitrix organizer instead.'
