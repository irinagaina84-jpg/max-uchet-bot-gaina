name: Setup Min Wei company

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy setup disabled
        run: echo 'Legacy Min Wei company setup is manual-only; current importer manages CRM idempotently.'
