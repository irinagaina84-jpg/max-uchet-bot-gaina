name: Setup Min Wei supplier

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy setup disabled
        run: echo 'Legacy Min Wei supplier setup is manual-only; current importer manages supplier data idempotently.'
