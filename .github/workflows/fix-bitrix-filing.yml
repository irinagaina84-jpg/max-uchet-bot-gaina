name: Fix Bitrix registry filing (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Registry filing correction is complete. Manual only.'
